[package]
name = "webapp-integration-frontend"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
telegram-webapp-sdk = { path = "../../..", features = ["macros"] }
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"